# change prefix to C-n
set -g prefix C-n
unbind C-b
bind C-n send-prefix

# for nicer vim colors
set -g default-terminal "screen-256color"

# start numbering windows from 1
set -g base-index 1
set -g pane-base-index 1

set -g history-limit 100000
